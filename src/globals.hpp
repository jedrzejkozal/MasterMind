/*Zmiennne globalne wykorzystywane w programie + deklaracje*/

//#include "deque.h"
#include <list>

void check(Color *code, Color *guess, const unsigned length, unsigned &correct, unsigned &correctPlace);
